import random

class Arrays :
    def __init__(self,):
        
        self.array_one = random.sample(range(1, 5000), 50)
        self.array_two = random.sample(range(1, 5000), 500)
        self.array_three = random.sample(range(1, 5000), 1000)